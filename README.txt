● The strategy used and the performances are described in detail in the file named "Relazione_Contest.pdf".
● The training script ("training.ipynb"), contains all the data processing used to train the segmentation network.
● The testing script ("testing.ipynb") has been used to perform automatic segmentation for each volume of the dataset (train,
validation and test). The script takes the MRI volumes as input and provide the atuomatic masks in the output.

The dataset has not been included as it is not public.
